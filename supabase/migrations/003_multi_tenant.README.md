# マルチテナントマイグレーションについて

`supabase db reset` / `migration up` は **ファイル名の辞書順** で実行されます。

`003_*.sql` だと `202604*.sql` / `20260501120000_*.sql` より **先** に走り、`profiles` が無い状態で `ALTER TABLE profiles` しようとして失敗します。

**実体:** `20260502120000_multi_tenant.sql`  
（全テーブル作成後に 1 本でマルチテナント化する想定です。）
